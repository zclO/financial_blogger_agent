# Financial Blogger Agent

一个以“可追溯、先审核、合规接入”为核心的 Tauri 2 桌面应用骨架，用于构建财经内容生产与发布工作流。

## 开发

```powershell
npm install
npm run tauri:dev
```

只验证前端：

```powershell
npm run check
```

打包 Windows 安装程序：

```powershell
npm run tauri:build
```

详细架构与编码规则请分别阅读 `docs/architecture.md` 和 `AGENTS.md`。
