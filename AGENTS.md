# Financial Blogger Agent — 编码规范

本文件适用于所有人类开发者和编码智能体。先阅读 `docs/architecture.md`，再修改代码。

## 非协商边界

- 不实现绕过登录、验证码、反爬或平台风控的采集/发帖逻辑；外部平台仅通过获授权的 API 或书面许可的数据源接入。
- API 密钥、OAuth token、Cookie、用户身份信息不得写入源码、日志、测试快照、前端环境变量或 Git。仅由 Rust 侧安全存储与按需注入。
- 发布默认进入审核队列。只有策略显式允许、来源可追溯且风险检查通过的低风险内容可自动发布。
- 每个内容结论必须附来源、原始时间、采集时间、许可状态和置信等级；LLM 输出不能替代事实来源。

## 目录责任

- `src/app/`：React 页面和组合逻辑，不直接调用第三方 API。
- `src/features/`：按业务域拆分的 UI、状态和用例适配层。
- `src/lib/`：Tauri 调用、纯工具和共享类型。
- `src-tauri/src/`：桌面端命令、密钥访问、任务编排与本地持久化。
- `workers/`：后续 Python 数据/生成 worker；其协议放在 `contracts/`，不得与 UI 直接耦合。
- `docs/`：架构与决策记录。改变边界时同步更新文档。

## 编码要求

- TypeScript 开启 strict；不使用 `any`。跨进程数据必须定义显式类型和可版本化 DTO。
- Rust 命令按领域模块组织，所有外部输入先校验；返回用户可读的受控错误，不泄漏密钥或底层堆栈。
- 一个提交/变更只解决一个意图；不得顺带重构不相关模块。
- 新功能至少覆盖：正常路径、授权/许可拒绝、网络失败或重试边界。
- 修改后运行相关检查；前端最少 `npm run check`，Rust 最少 `cargo check --manifest-path src-tauri/Cargo.toml`。

## 提交前清单

1. 无密钥、Cookie、真实用户内容或受版权保护的原文进入仓库。
2. 新数据源的授权方式、速率限制和归属展示已记录。
3. 涉及自动发布的改动保留人工暂停开关和审计记录。
4. 文案明确标注“信息整理，不构成投资建议”。
