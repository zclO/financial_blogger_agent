# 环境变量示例 / Environment Variables Example
#
# 复制此文件为 .env 并填入实际值：
# Copy this file to .env and fill in actual values:
#   cp .env.example .env
#
# ⚠️ 请勿将 .env 文件提交到版本控制！
# ⚠️ Do NOT commit .env to version control!

# ─────────────────────────────────────────────
# 开发环境 / Development
# ─────────────────────────────────────────────

# Vite 开发服务器端口（默认 1420，与 tauri.conf.json 一致）
# VITE_DEV_PORT=1420

# ─────────────────────────────────────────────
# 测试用 API 端点 / Test API Endpoints
# ─────────────────────────────────────────────

# 测试用 LLM API 端点（仅用于本地开发测试）
# TEST_LLM_API_ENDPOINT=http://localhost:11434/v1/chat/completions

# 测试用 LLM API 密钥（仅用于本地开发，生产环境由 Rust 侧安全存储）
# TEST_LLM_API_KEY=your-test-key-here

# ─────────────────────────────────────────────
# 注意事项 / Notes
# ─────────────────────────────────────────────
#
# 1. 生产环境的 API 密钥由 Rust 侧安全存储，
#    不通过环境变量传递。
#    Production API keys are securely stored on the Rust side,
#    not passed via environment variables.
#
# 2. 上述 TEST_* 变量仅用于本地开发测试，
#    不应包含真实凭证。
#    TEST_* variables are for local development only,
#    do not include real credentials.
#
# 3. 更多信息请参阅 CONTRIBUTING.md 的安全边界章节。
#    See the Security Boundaries section in CONTRIBUTING.md.
